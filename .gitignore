node_modules/
dist/

# secrets — never commit (Kit credentials live in .env.local / Vercel env vars)
.env
.env.*

# The Kit handover docs in this folder contain the API secret — folder stays
# ignored; the safe content docs are whitelisted below.
Quiz Questions and logic/*
*.docx

# …content docs the team DOES need in the repo:
!Quiz Questions and logic/Result Page Copy.docx
!Quiz Questions and logic/Quiz Logics.docx
!Quiz Questions and logic/Quiz Questions.docx
!Quiz Questions and logic/Quiz building plan and process.docx

# local OS noise
Thumbs.db
.DS_Store
